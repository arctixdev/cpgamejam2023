using System;
using System.Collections;
using System.Collections.Generic;
using Unity.Mathematics;
using UnityEngine;

public class basicEnemyBehavierOld : MonoBehaviour
{
    //public Queue<Vector2> targetQueue;

    //public event Action<Vector2> reachedTarget;
    //public float distanceToTarget;

    // Start is called before the first frame update
    //void Start()
    //{
        
    //}

    // Update is called once per frame
    //void Update()
    //{
    //    distanceToTarget = math.distance(v3v2(transform.position), targetQueue.Peek());
    //    if (distanceToTarget < 1)
    //    {
    //        reachedTarget(targetQueue.Dequeue());
    //    }
    //}

    //public void addForceTowardsTarget(float force)
    //{

    //}

    //public void turnTowardsTarget(float amount)
    //{
    //    Vector2 targetRotation = (v3v2(transform.position)-targetQueue.Peek().normalized);
    //    transform.rotation
    //}

    //public void ResetAll()
    //{
        
    //}

    //private Vector2 v3v2(Vector3 v)
    //{
    //    return new Vector2 (v.x, v.y);
    //}
}
